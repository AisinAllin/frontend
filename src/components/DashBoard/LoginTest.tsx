/* eslint-disable*/
import React from 'react';

const LoginTest = (props:any) => (
  <div>
    <button onClick={() => { props.handleClick; }}> get name </button>
  </div>
);

export default LoginTest;
